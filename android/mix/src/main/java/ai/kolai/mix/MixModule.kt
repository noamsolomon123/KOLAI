package ai.kolai.mix

/**
 * Placeholder marker for the :mix module.
 *
 * Scope (per spec section 5): FloatArray DSP (crossfade/duck/normalize); BlockEncoder (to .m4a).
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object MixModule
