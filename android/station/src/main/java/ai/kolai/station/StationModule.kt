package ai.kolai.station

/**
 * Placeholder marker for the :station module.
 *
 * Scope (per spec section 5): SetlistPlanner, RollingPlanner, DJBrain, BlockRenderer, StationEngine (coroutine).
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object StationModule
