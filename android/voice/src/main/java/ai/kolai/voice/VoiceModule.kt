package ai.kolai.voice

/**
 * Placeholder marker for the :voice module.
 *
 * Scope (per spec section 5): GeminiTextClient, GeminiTtsSynth (REST), VoiceRenderer, pcmToWav.
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object VoiceModule
