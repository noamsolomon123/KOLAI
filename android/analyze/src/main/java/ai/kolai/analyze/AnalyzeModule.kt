package ai.kolai.analyze

/**
 * Placeholder marker for the :analyze module.
 *
 * Scope (per spec section 5): AudioDecoder (MediaCodec to PCM); Analyzer (JNI to Essentia); octave/confidence guards.
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object AnalyzeModule
