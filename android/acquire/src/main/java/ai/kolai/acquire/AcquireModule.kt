package ai.kolai.acquire

/**
 * Placeholder marker for the :acquire module.
 *
 * Scope (per spec section 5): AudioSource interface; NewPipeSource, LocalFilesSource; candidate scoring; cache.
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object AcquireModule
