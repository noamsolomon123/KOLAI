package ai.kolai.core

/**
 * Placeholder marker for the :core module.
 *
 * Scope (per spec section 5): models (TrackAnalysis, Song, Block), Camelot/keys, config, secure key store.
 * This is an empty skeleton created in Task 0.1; real implementation follows in
 * the later phases of the plan.
 */
internal object CoreModule
