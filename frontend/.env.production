# Production build target for the phone (APK + installed PWA).
# The app runs on the OnePlus 15 and reaches the PC backend over the LAN.
# Chosen IP: the PC's real DHCP LAN address (Ethernet adapter).
# Backend must run with: uvicorn radioai.server:app --host 0.0.0.0 --port 8000
VITE_API_BASE=http://10.100.102.123:8000
